# The `usethat` python package

`usethat` is inspired by `R`'s `usethis`.

## Installation

```shell
pip install git+https://github.com/dheimgartner/usethat.git
```

## Usage

Provides a cli entry point:

```shell
usethat scaffold
```